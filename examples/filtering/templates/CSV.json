{
    "name": "CSV",
    "templates": {
        "ListToCSV": {
            "code": "class ListToCSV:\n    def __init__(self, alist, header):\n        content = header + \"\\n\"\n        for item in alist:\n            content += str(item)[1:-1] + \"\\n\"\n        self.csv = content\n",
            "collection_name": "CSV",
            "documentation": "A description of the ListToCSV node's behavior.",
            "inputs": [
                "alist",
                "header"
            ],
            "name": "ListToCSV",
            "outputs": [
                "csv"
            ]
        }
    }
}